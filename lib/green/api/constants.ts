/** Anonymous IP quota — public Green score reads (no API key). */
export const GREEN_ANON_DAILY_LIMIT = 100;

/** Max compare ids per bulk GET without API key. */
export const GREEN_ANON_BULK_MAX_IDS = 5;

/** Max compare ids per bulk GET with free API key. */
export const GREEN_FREE_BULK_MAX_IDS = 20;

/** Max items per CQS batch on free tier. */
export const GREEN_FREE_BATCH_MAX_ITEMS = 10;

/** Max items per CQS batch on premium+ tiers. */
export const GREEN_PREMIUM_BATCH_MAX_ITEMS = 50;

export const GREEN_API_DOCS_ROUTE = "/green/api";
export const GREEN_API_OPENAPI_PATH = "/api/green/openapi";
